import styled from 'styled-components';

const SBasketCount = styled.span`
  position: absolute;
  display: inline-block;
  padding: 5px;
  top: 40px;
  right: 30px;
  z-index: 10;
  color: green;
  background: yellow;
`;

export default SBasketCount;
