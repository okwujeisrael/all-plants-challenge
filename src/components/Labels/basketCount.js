import SBasketCount from './labels.styled';

// eslint-disable-next-line react/prop-types
const BasketCount = ({ count }) => (
  <SBasketCount>
    {count}
  </SBasketCount>
);

export default BasketCount;
