import styled from 'styled-components';

const SLoading = styled.div`
  text-align: center;
  color: green;
  font-weight: bold;
  font-size: 3rem;
  letter-spacing: 1rem;
`;

export default SLoading;
